---
export const prerender = false;
import BaseLayout from '../../layouts/BaseLayout.astro';

const tools = [
  {
    href: '/keystatic/branch/master/collection/articles',
    title: 'Keystatic',
    description: 'Full editor: create, edit, and delete individual articles field by field.',
    protected: false,
  },
  {
    href: '/admin/bulk',
    title: 'Bulk Articles',
    description: 'Select multiple articles to change their status or delete them in bulk.',
    protected: true,
  },
  {
    href: '/admin/import',
    title: 'Import',
    description: 'Paste or drag .mdx files to publish them directly to the repository.',
    protected: true,
  },
  {
    href: '/admin/export',
    title: 'Export',
    description: 'Download the up-to-date list of existing tags and series, in Markdown.',
    protected: true,
  },
];
---

<BaseLayout title="Admin">
  <div class="admin-hub">
    <h1>Admin</h1>
    <p class="intro">All the tools to manage the blog, in one place.</p>

    <div class="cards">
      {tools.map(tool => (
        <a class="card" href={tool.href}>
          <div class="card-top">
            <span class="card-title">{tool.title}</span>
            {tool.protected && <span class="lock" title="Requires ADMIN_TOKEN">🔒</span>}
          </div>
          <p class="card-desc">{tool.description}</p>
        </a>
      ))}
    </div>
  </div>
</BaseLayout>

<style>
  .admin-hub h1 {
    font-family: 'Lora', serif;
    font-size: 2rem;
    margin: 0 0 8px 0;
  }

  .intro {
    font-family: 'DM Sans', sans-serif;
    color: var(--color-muted);
    margin: 0 0 28px 0;
    font-size: 0.95rem;
  }

  .cards {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 14px;
  }

  @media (max-width: 639px) {
    .cards {
      grid-template-columns: 1fr;
    }
  }

  .card {
    display: block;
    border: 1px solid var(--color-border);
    border-radius: 10px;
    padding: 20px;
    text-decoration: none;
    color: inherit;
    transition: border-color 0.15s, transform 0.1s;
  }

  .card:hover {
    border-color: var(--color-accent);
    text-decoration: none;
  }

  .card-top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 8px;
    margin-bottom: 6px;
  }

  .card-title {
    font-family: 'Lora', serif;
    font-size: 1.1rem;
    font-weight: 700;
    color: var(--color-text);
  }

  .lock {
    font-size: 0.85rem;
    opacity: 0.6;
  }

  .card-desc {
    font-family: 'DM Sans', sans-serif;
    font-size: 0.85rem;
    color: var(--color-muted);
    margin: 0;
    line-height: 1.5;
  }
</style>
